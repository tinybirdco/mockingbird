{
  "name": "mockingbird-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf .turbo && cd packages/mockingbird && pnpm clean && cd ../../apps/cli && pnpm clean && cd ../web && pnpm clean"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.2.0"
}
